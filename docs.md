# 📖 Документация BuyerPlugin (v2.0)

**BuyerPlugin** — это профессиональное решение для Minecraft серверов, которое превращает обычную скупку предметов в полноценную экономическую систему с ротацией, категориями, прогрессией и авто-продажей.

---

## 🎮 Команды и Права
*   `/скупщик` (алиас `/buyer`) — Открыть главное меню.
*   `/buyer reload` — Перезагрузить все конфиги (`buyer.admin`).
*   `/buyer rotate` — Принудительно обновить товары (`buyer.admin`).
*   `/buyer resetlimits` — Сбросить все лимиты скупки (`buyer.admin`).
*   `/buyer stats` — Посмотреть статистику продаж.

---

## 📂 Подробная настройка файлов

### 1️⃣ `config.yml` — Ядро плагина
*   **`database`**: Настройка SQLite (локально) или MySQL (для сети серверов).
*   **`economy`**: Переключение между `eco` (встроенная) и `money` (Vault).
*   **`rotation`**: Настройка автоматического обновления товаров (интервал, мин/макс количество).
*   **`multiplier`**: Система уровней. За продажу N предметов игрок получает +X% к цене навсегда.
*   **`auto-sell`**: 
    *   `interval`: Частота продажи (сек).
    *   `display`: **Полная кастомизация** префиксов `[✓]/[✗]` и Lore предметов в меню.

### 2️⃣ `menus.yml` — Визуальная часть (UI)
Здесь настраиваются все меню (`main_menu`, `catalog_menu`, `categories_menu`, `autosell_menu`).
*   **Слоты**: Можно переместить любую кнопку или декорацию.
*   **Материалы**: Любой предмет, включая головы с текстурами (`texture: base64`).
*   **Цвета**: Поддержка HEX (`#RRGGBB`) во всех заголовках и описаниях.

### 3️⃣ `categories.yml` — Категории предметов
*   **`preview`**: Настройка слотов для быстрого просмотра товаров (ЛКМ по категории).
*   **Группировка**: Создавайте свои категории (например, "Ферма", "Шахта") и добавляйте туда любые материалы Minecraft.

### 4️⃣ `points.yml` — Система Бонусных Очков
За каждую продажу игрок получает очки.
*   **`global-multiplier`**: Множитель для всех очков.
*   **`items`**: Список предметов, за которые дают фиксированное количество очков.
*   **`ratio-per-money`**: Коэффициент для остальных предметов (очки = цена * коэф).

### 5️⃣ `shop.yml` — Магазин за очки
Игроки тратят очки на:
*   **Команды**: Выдача привилегий, валюты, ключей.
*   **Предметы**: Редкие ресурсы или артефакты.

### 6️⃣ `wipe.yml` — Динамическая экономика
Цены на сервере могут расти со временем после вайпа.
*   **`daily-increase-percent`**: На сколько процентов дорожают товары каждый день.
*   **`max-total-bonus-percent`**: Ограничение роста (например, максимум +100%).

---

## 💡 Полезные советы по настройке
1.  **HEX-цвета**: Используйте формат `#55FFFF` для создания градиентов и красивых заголовков.
2.  **Авто-скупщик**: Чтобы игроки могли выбирать предметы, убедитесь, что в `categories.yml` настроены `preview.slots`.
3.  **Лимиты**: Если хотите ограничить скупку редких вещей, настройте раздел `limits` в `config.yml`.
4.  **Смена позиции**: Если кнопка "Назад" мешает, просто смените `slot` в `menus.yml`.

---

## 🔗 Интеграция с PlaceholderAPI
*   `%darkseller_points%` — Баланс очков.
*   `%darkseller_multiplier%` — Текущий множитель цены.
*   `%darkseller_level%` — Уровень прогрессии.
